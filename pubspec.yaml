name: my_new_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  camera: ^0.10.6
  path_provider: ^2.1.5
  path: ^1.9.0
  intl: ^0.20.2  # 保持 0.20.2，与 Flutter SDK 一致
  xml: ^6.4.2
  flutter_foreground_task: ^9.2.0
  flutter_native_splash: ^2.4.1
  extended_image: ^10.0.1
  flutter_staggered_grid_view: ^0.7.0
  image: ^4.7.2
  file_picker: ^10.3.8
  share_plus: ^12.0.1
  permission_handler: ^11.3.1
  # === 日记本依赖 ===
  flutter_riverpod: ^2.5.1
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  uuid: ^4.3.0
  flutter_quill: ^11.5.0  # ⬅️ 升级到最新版

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true